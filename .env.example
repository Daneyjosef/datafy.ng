# Domains & Hosting — provider API credentials
# Leave unset to run the domain search API in mock mode (safe default).
# Set these once your reseller applications are approved and you have real docs/keys.

WHOGOHOST_API_KEY=
WHOGOHOST_RESELLER_ID=
WHOGOHOST_API_BASE_URL=

RESELLERCLUB_API_KEY=
RESELLERCLUB_RESELLER_ID=
RESELLERCLUB_API_BASE_URL=

# Payment (wire up once domain registration is actually fulfillable — see api/domains/register.ts)
PAYSTACK_SECRET_KEY=
FLUTTERWAVE_SECRET_KEY=
